<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_search"
        android:icon="@drawable/ic_search"
        android:title="search"
        app:actionViewClass="androidx.appcompat.widget.SearchView"
        app:showAsAction="always|collapseActionView" />

    <item
        android:icon="@drawable/ic_sort"
        android:title="itemSort"
        app:showAsAction="ifRoom">

        <menu>

            <item
                android:id="@+id/action_sort_name"
                android:title="sort by name" />

            <item
                android:id="@+id/action_sort_date"
                android:title="sort by date" />

        </menu>
    </item>

    <item
        android:id="@+id/action_hide_complete_tasks"
        android:checkable="true"
        android:title="Hide completed"
        app:showAsAction="never" />
    <item
        android:id="@+id/action_delete_all_complete_task"
        android:title="Delete all completed"
        app:showAsAction="never" />

</menu>